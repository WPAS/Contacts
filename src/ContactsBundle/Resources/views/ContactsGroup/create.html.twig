{% extends "::base.html.twig" %}

{% block body %}
    {{ parent() }}
    <div class="row">
        <div class="small-6 small-offset-3 columns">
            {{ form_start(form) }}
            {{ form_widget(form) }}
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}